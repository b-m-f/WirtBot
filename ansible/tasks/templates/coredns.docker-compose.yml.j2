version: "3"
services:
  coredns:
    image: coredns/coredns
    ports:
      - "10.10.0.1:53:53/udp"
      - "10.10.0.1:5300:5300/udp"
    restart: "unless-stopped"
    network_mode: "host"
    working_dir: /v
    volumes:
      - ./data:/v
    dns:
      - 1.1.1.1


